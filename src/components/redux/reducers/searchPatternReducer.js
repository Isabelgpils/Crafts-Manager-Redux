// const selectPatternReducer = (state = null, action) => {
// 	console.log('reducer', state);
// 	switch (action.type) {
// 		case 'PATTERN_SELECTED':
// 			const prevDmcSelection = (state && state.selectPattern) || [];
// 			return { ...state, selectPattern: [...prevPatternSelection, action.payload] };


// 		default:
// 			return state;
// 	}
// };
// export default searchPatternReducer;